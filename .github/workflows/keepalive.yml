name: Render Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -s -o /dev/null -w "%{http_code}" https://match-decoded-api.onrender.com/health
